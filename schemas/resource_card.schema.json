{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/JHUNyizheng/awesome-wireless-coverage-intelligence/main/schemas/resource_card.schema.json",
  "title": "Radio Map and CKM Open-Artifact Resource Card",
  "type": "object",
  "required": [
    "name",
    "canonical_url",
    "resource_type",
    "publication_category",
    "map_target",
    "dimensionality",
    "artifact_support",
    "availability_status",
    "license",
    "version_or_commit",
    "last_verified",
    "reproduction_scope",
    "recommended_use",
    "caveat"
  ],
  "properties": {
    "name": { "type": "string" },
    "canonical_url": { "type": "string", "format": "uri" },
    "url": { "type": "string", "format": "uri", "deprecated": true },
    "paper_or_project": { "type": "string" },
    "resource_type": {
      "type": "array",
      "items": {
        "enum": ["paper", "dataset", "code", "model", "demo", "api", "evaluation-protocol", "toolchain", "index", "tutorial"]
      },
      "minItems": 1,
      "uniqueItems": true
    },
    "publication_category": {
      "enum": ["published", "foundation-toolchain", "preprint", "resource-first", "community-index", "unknown"]
    },
    "map_target": {
      "type": "array",
      "items": {
        "enum": ["pathloss", "RSS", "RSRP", "SINR", "REM", "spectrum", "CKM", "beam", "delay", "angle", "LoS", "uncertainty", "freshness", "task-utility"]
      },
      "minItems": 1,
      "uniqueItems": true
    },
    "dimensionality": {
      "type": "array",
      "items": {
        "enum": ["2D", "2.5D", "height-aware-3D", "volumetric-3D", "3D-by-3D", "multiband", "dynamic", "low-altitude", "indoor", "outdoor"]
      },
      "minItems": 1,
      "uniqueItems": true
    },
    "artifact_support": {
      "type": "object",
      "required": ["dataset", "code", "pretrained_models", "demo_or_app", "fixed_splits", "license_or_provenance", "downstream_scripts"],
      "properties": {
        "dataset": { "enum": ["yes", "partial", "linked", "no", "not-applicable"] },
        "code": { "enum": ["yes", "partial", "linked", "no", "not-applicable"] },
        "pretrained_models": { "enum": ["yes", "partial", "request-based", "no", "not-applicable"] },
        "demo_or_app": { "enum": ["yes", "partial", "linked", "no", "not-applicable"] },
        "fixed_splits": { "enum": ["yes", "partial", "no", "not-applicable"] },
        "license_or_provenance": { "enum": ["strong", "partial", "weak", "unknown"] },
        "downstream_scripts": { "enum": ["yes", "partial", "no", "not-applicable"] }
      },
      "additionalProperties": false
    },
    "availability_status": {
      "enum": ["open", "restricted", "unavailable", "link-broken", "unclear-license"]
    },
    "license": { "type": "string", "minLength": 1 },
    "version_or_commit": { "type": "string", "minLength": 1 },
    "last_verified": { "type": "string", "format": "date" },
    "reproduction_scope": { "type": "string", "minLength": 1 },
    "verification_note": { "type": "string" },
    "recommended_use": { "type": "string" },
    "caveat": { "type": "string" },
    "last_checked": { "type": "string", "format": "date", "deprecated": true }
  },
  "additionalProperties": false
}
